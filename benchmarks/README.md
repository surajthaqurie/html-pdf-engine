# Benchmarks Suite: html-pdf-engine

This suite measures render speed (ops/sec), memory overhead (MB heap delta), and compressed binary stream output sizes.

## Execution

Run all benchmarks using npm:

```bash
npm run benchmark
```
