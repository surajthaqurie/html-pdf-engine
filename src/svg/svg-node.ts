import { ElementNode } from "../html/dom/node.js";

export class SvgElementNode extends ElementNode {
  // Can add any custom SVG helpers here if needed, otherwise it behaves exactly like ElementNode.
}
